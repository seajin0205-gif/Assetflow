# Copy to `.env` for local development.
# Public / non-secret values only. Do not put private keys here.

# Optional Builder.io public key (if used)
VITE_PUBLIC_BUILDER_KEY=

# Express /api/ping response message
PING_MESSAGE="ping pong"
